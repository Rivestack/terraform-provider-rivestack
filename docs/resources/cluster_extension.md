---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "rivestack_cluster_extension Resource - terraform-provider-rivestack"
subcategory: ""
description: |-
  Manages a PostgreSQL extension on a Rivestack HA cluster. Note: extensions cannot be removed from a running cluster; destroying this resource removes it from Terraform state only.
---

# rivestack_cluster_extension (Resource)

Manages a PostgreSQL extension on a Rivestack HA cluster. Note: extensions cannot be removed from a running cluster; destroying this resource removes it from Terraform state only.

## Example Usage

```terraform
resource "rivestack_cluster_extension" "example" {
  cluster_id = rivestack_cluster.example.id
  extension  = "vector"
  database   = rivestack_cluster_database.example.name
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `cluster_id` (String) ID of the cluster.
- `extension` (String) PostgreSQL extension name (e.g., vector, postgis, uuid-ossp).

### Optional

- `database` (String) Database to install the extension on. Defaults to the cluster's default database.

### Read-Only

- `id` (String) Resource identifier (cluster_id/extension/database).
